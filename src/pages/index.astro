---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { EMAIL, TEAM, FORMSPREE_ID } from "../consts";
import { especialidades, serviciosApoyo } from "../data/servicios";

// Configurable desde un .env (PUBLIC_FORMSPREE_ID=xxxxx); por defecto usa el
// ID de consts. Sin ninguno, el formulario cae con elegancia a un mailto:.
const formspreeId = import.meta.env.PUBLIC_FORMSPREE_ID ?? FORMSPREE_ID;
const formAction = formspreeId
  ? `https://formspree.io/f/${formspreeId}`
  : `mailto:${EMAIL}`;

// Tras enviar, Formspree redirige aquí (página de gracias propia).
const graciasUrl = new URL("/gracias", Astro.site).toString();

const problemas = [
  "Un prototipo de producto para enseñar a inversores o primeros clientes",
  "Dispositivos conectados (IoT) e integración de sensores",
  "Electrónica que no funciona como debería",
  "Automatización de equipos",
  "Mejora de un producto existente",
];

const proceso = [
  {
    n: "01",
    titulo: "Estudio técnico inicial",
    texto: "Analizamos tu necesidad y definimos la mejor solución antes de empezar.",
  },
  {
    n: "02",
    titulo: "Diseño de la solución",
    texto: "Arquitectura, electrónica y software pensados para ser fiables.",
  },
  {
    n: "03",
    titulo: "Desarrollo del prototipo",
    texto: "Montaje y programación del sistema funcional.",
  },
  {
    n: "04",
    titulo: "Entrega y pruebas",
    texto: "Validación, ajustes finales y documentación del resultado.",
  },
];
---

<Layout>
  <Header />

  <main id="top">
    <!-- ── 1 · Cabecera ──────────────────────────────────── -->
    <section class="relative overflow-hidden pt-36 pb-24 md:pt-44 md:pb-32">
      <div class="grid-bg absolute inset-0" aria-hidden="true"></div>
      <div class="glow absolute inset-0" aria-hidden="true"></div>
      <div class="relative mx-auto max-w-6xl px-5 text-center">
        <p class="mb-5 inline-flex items-center gap-2 rounded-full border border-[var(--color-line)] bg-[var(--color-surface)] px-4 py-1.5 text-xs font-medium text-[var(--color-muted)]">
          <span class="h-1.5 w-1.5 rounded-full bg-[var(--color-accent)]"></span>
          Hardware · Diseño · IoT a medida
        </p>
        <h1 class="mx-auto max-w-4xl text-4xl font-extrabold leading-[1.05] tracking-tight sm:text-6xl md:text-7xl">
          Diseñamos soluciones <br class="hidden sm:block" />
          electrónicas que
          <span class="bg-gradient-to-r from-[var(--color-accent)] to-[var(--color-accent-2)] bg-clip-text text-transparent">funcionan</span>.
        </h1>
        <p class="mx-auto mt-7 max-w-2xl text-lg text-[var(--color-muted)]">
          Llevamos tu idea de hardware del concepto al prototipo funcional que
          puedes enseñar a inversores y clientes. Electrónica, diseño, firmware
          y gestión del proyecto — con un único equipo.
        </p>
        <div class="mt-10 flex flex-wrap items-center justify-center gap-4">
          <a
            href="#contacto"
            class="rounded-xl bg-[var(--color-accent)] px-7 py-3.5 font-semibold text-[#04221d] transition-transform hover:-translate-y-0.5"
          >
            Solicitar estudio técnico
          </a>
          <a
            href="#servicios"
            class="rounded-xl border border-[var(--color-line)] px-7 py-3.5 font-semibold text-[var(--color-ink)] transition-colors hover:border-[var(--color-accent)]/60"
          >
            Ver servicios
          </a>
        </div>
      </div>
    </section>

    <!-- ── 2 · Qué hacemos ───────────────────────────────── -->
    <section id="que-hacemos" class="border-t border-[var(--color-line)]/60 py-20 md:py-28">
      <div class="mx-auto grid max-w-6xl gap-12 px-5 md:grid-cols-2 md:items-center">
        <div>
          <h2 class="text-3xl font-bold tracking-tight md:text-4xl">
            De la idea al prototipo que funciona
          </h2>
          <p class="mt-6 text-lg leading-relaxed text-[var(--color-muted)]">
            Cubrimos toda la cadena de un producto electrónico —electrónica,
            firmware, diseño y gestión— en un mismo equipo. Así no tienes que
            coordinar a varios proveedores en la fase más delicada: validar que
            tu idea es viable.
          </p>
          <p class="mt-4 text-lg leading-relaxed text-[var(--color-muted)]">
            Trabajamos con <strong class="text-[var(--color-ink)]">startups de
            hardware e IoT</strong> que necesitan un primer prototipo, y con
            <strong class="text-[var(--color-ink)]">empresas y profesionales</strong>
            que buscan una solución electrónica fiable y bien diseñada.
          </p>
          <p class="mt-4 rounded-xl border border-[var(--color-line)] bg-[var(--color-surface)] p-4 text-sm leading-relaxed text-[var(--color-muted)]">
            <strong class="text-[var(--color-ink)]">Hasta dónde llegamos:</strong>
            nuestro punto fuerte es la fase de concepto a prototipo validado. Si
            después necesitas industrialización o certificación, te lo decimos
            con claridad desde el estudio técnico.
          </p>
        </div>
        <div class="rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-8">
          <h3 class="text-xl font-semibold">¿En qué podemos ayudarte?</h3>
          <ul class="mt-6 space-y-3.5">
            {problemas.map((p) => (
              <li class="flex items-start gap-3 text-[var(--color-muted)]">
                <span class="mt-1 grid h-5 w-5 shrink-0 place-items-center rounded-full bg-[var(--color-accent)]/15 text-[var(--color-accent)]" aria-hidden="true">
                  <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                </span>
                {p}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    <!-- ── 3 · Servicios ─────────────────────────────────── -->
    <section id="servicios" class="border-t border-[var(--color-line)]/60 py-20 md:py-28">
      <div class="mx-auto max-w-6xl px-5">
        <div class="max-w-2xl">
          <h2 class="text-3xl font-bold tracking-tight md:text-4xl">Servicios</h2>
          <p class="mt-4 text-lg text-[var(--color-muted)]">
            Cubrimos tu producto de principio a fin: lo técnico, el diseño y la
            gestión. El software de apoyo lo completa de extremo a extremo.
          </p>
        </div>

        <h3 class="mt-12 text-sm font-semibold uppercase tracking-wider text-[var(--color-accent)]">
          Especialidad
        </h3>
        <div class="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
          {especialidades.map((s) => (
            <a
              href={`/servicios/${s.slug}`}
              class="group flex flex-col rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-7 transition-colors hover:border-[var(--color-accent)]/50"
            >
              <h4 class="text-lg font-semibold text-[var(--color-ink)]">{s.titulo}</h4>
              <p class="mt-3 flex-1 text-[var(--color-muted)]">{s.resumen}</p>
              <span class="mt-5 inline-flex items-center gap-1.5 text-sm font-medium text-[var(--color-accent)]">
                Ver más
                <svg class="transition-transform group-hover:translate-x-0.5" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
              </span>
            </a>
          ))}
        </div>

        <h3 class="mt-12 text-sm font-semibold uppercase tracking-wider text-[var(--color-muted)]">
          Servicios de apoyo
        </h3>
        <div class="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
          {serviciosApoyo.map((s) => (
            <a
              href={`/servicios/${s.slug}`}
              class="group flex flex-col rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-7 transition-colors hover:border-[var(--color-accent)]/50"
            >
              <h4 class="text-lg font-semibold text-[var(--color-ink)]">{s.titulo}</h4>
              <p class="mt-3 flex-1 text-[var(--color-muted)]">{s.resumen}</p>
              <span class="mt-5 inline-flex items-center gap-1.5 text-sm font-medium text-[var(--color-accent)]">
                Ver más
                <svg class="transition-transform group-hover:translate-x-0.5" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
              </span>
            </a>
          ))}
        </div>

        <div class="mt-10 rounded-2xl border border-[var(--color-accent)]/30 bg-gradient-to-br from-[var(--color-surface-2)] to-[var(--color-surface)] p-8 md:p-10">
          <h3 class="text-xl font-semibold text-[var(--color-accent)]">Estudio técnico inicial</h3>
          <p class="mt-3 max-w-3xl text-lg leading-relaxed text-[var(--color-muted)]">
            Antes de empezar cualquier proyecto realizamos un estudio técnico
            inicial para definir la mejor solución, acotar el alcance y darte un
            presupuesto claro.
          </p>
        </div>
      </div>
    </section>

    <!-- ── 4 · Cómo trabajamos ───────────────────────────── -->
    <section id="proceso" class="border-t border-[var(--color-line)]/60 py-20 md:py-28">
      <div class="mx-auto max-w-6xl px-5">
        <div class="max-w-2xl">
          <h2 class="text-3xl font-bold tracking-tight md:text-4xl">Cómo trabajamos</h2>
          <p class="mt-4 text-lg text-[var(--color-muted)]">
            Trabajamos con procesos claros para asegurar resultados fiables.
          </p>
        </div>
        <ol class="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
          {proceso.map((p) => (
            <li class="relative rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-7">
              <span class="text-3xl font-extrabold text-[var(--color-accent)]/30">{p.n}</span>
              <h3 class="mt-3 text-lg font-semibold">{p.titulo}</h3>
              <p class="mt-2 text-sm leading-relaxed text-[var(--color-muted)]">{p.texto}</p>
            </li>
          ))}
        </ol>
      </div>
    </section>

    <!-- ── 5 · Sobre nosotros ────────────────────────────── -->
    <section id="sobre" class="border-t border-[var(--color-line)]/60 py-20 md:py-28">
      <div class="mx-auto max-w-3xl px-5 text-center">
        <h2 class="text-3xl font-bold tracking-tight md:text-4xl">
          Somos {TEAM.map((p) => p.nombre.split(" ")[0]).join(" y ")}
        </h2>
        <p class="mt-7 text-lg leading-relaxed text-[var(--color-muted)]">
          Detrás de LUCOM somos dos. Trabajas directamente con quienes diseñan y
          construyen tu proyecto, sin intermediarios ni capas comerciales, y con
          un equipo que cubre el producto completo: lo técnico, el diseño y la
          gestión.
        </p>

        <div class="mt-10 grid gap-5 sm:grid-cols-2">
          {TEAM.map((p) => (
            <div class="flex flex-col rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-7 text-left">
              <div class="flex items-center gap-4">
                <span
                  class="grid h-14 w-14 shrink-0 place-items-center rounded-full border border-[var(--color-accent)]/40 text-lg font-extrabold text-[var(--color-accent)]"
                  aria-hidden="true"
                >
                  {p.iniciales}
                </span>
                <div>
                  <h3 class="text-lg font-semibold text-[var(--color-ink)]">{p.nombre}</h3>
                  <p class="text-sm text-[var(--color-muted)]">{p.rol}</p>
                </div>
              </div>
              {p.bio && (
                <p class="mt-5 flex-1 text-sm leading-relaxed text-[var(--color-muted)]">{p.bio}</p>
              )}
              {p.linkedin && (
                <a
                  href={p.linkedin}
                  target="_blank"
                  rel="noopener"
                  class="mt-5 inline-flex items-center gap-1.5 text-sm font-medium text-[var(--color-accent)]"
                  aria-label={`LinkedIn de ${p.nombre}`}
                >
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M20.45 20.45h-3.56v-5.57c0-1.33-.02-3.04-1.85-3.04-1.85 0-2.14 1.45-2.14 2.94v5.67H9.34V9h3.42v1.56h.05c.48-.9 1.64-1.85 3.37-1.85 3.6 0 4.27 2.37 4.27 5.46v6.28zM5.34 7.43a2.07 2.07 0 1 1 0-4.14 2.07 2.07 0 0 1 0 4.14zM7.12 20.45H3.55V9h3.57v11.45zM22.22 0H1.77C.79 0 0 .77 0 1.72v20.56C0 23.23.79 24 1.77 24h20.45c.98 0 1.78-.77 1.78-1.72V1.72C24 .77 23.2 0 22.22 0z"/></svg>
                  LinkedIn
                </a>
              )}
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ── 6 · Contacto ──────────────────────────────────── -->
    <section id="contacto" class="border-t border-[var(--color-line)]/60 py-20 md:py-28">
      <div class="mx-auto grid max-w-6xl gap-12 px-5 md:grid-cols-2">
        <div>
          <h2 class="text-3xl font-bold tracking-tight md:text-4xl">
            Cuéntanos tu proyecto
          </h2>
          <p class="mt-6 text-lg leading-relaxed text-[var(--color-muted)]">
            Escríbenos y te responderemos en menos de 24 horas.
          </p>
          <a
            href={`mailto:${EMAIL}`}
            class="mt-8 inline-flex items-center gap-3 rounded-xl border border-[var(--color-line)] bg-[var(--color-surface)] px-5 py-4 font-medium transition-colors hover:border-[var(--color-accent)]/60"
          >
            <span class="grid h-9 w-9 place-items-center rounded-lg bg-[var(--color-accent)]/15 text-[var(--color-accent)]" aria-hidden="true">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-10 5L2 7"/></svg>
            </span>
            {EMAIL}
          </a>
        </div>

        <div>
        <form
          id="contacto-form"
          action={formAction}
          method="POST"
          class="rounded-2xl border border-[var(--color-line)] bg-[var(--color-surface)] p-7 md:p-8"
        >
          {formspreeId && (
            <>
              <input type="hidden" name="_next" value={graciasUrl} />
              <input type="hidden" name="_subject" value="Nuevo mensaje desde lucom.es" />
            </>
          )}
          <div class="space-y-5">
            <div>
              <label for="nombre" class="mb-2 block text-sm font-medium">Nombre</label>
              <input
                id="nombre"
                name="nombre"
                type="text"
                required
                autocomplete="name"
                class="w-full rounded-lg border border-[var(--color-line)] bg-[var(--color-base)] px-4 py-3 text-[var(--color-ink)] outline-none transition-colors focus:border-[var(--color-accent)]"
              />
            </div>
            <div>
              <label for="email" class="mb-2 block text-sm font-medium">Email</label>
              <input
                id="email"
                name="email"
                type="email"
                required
                autocomplete="email"
                class="w-full rounded-lg border border-[var(--color-line)] bg-[var(--color-base)] px-4 py-3 text-[var(--color-ink)] outline-none transition-colors focus:border-[var(--color-accent)]"
              />
            </div>
            <div>
              <label for="mensaje" class="mb-2 block text-sm font-medium">Mensaje</label>
              <textarea
                id="mensaje"
                name="mensaje"
                rows="4"
                required
                class="w-full resize-y rounded-lg border border-[var(--color-line)] bg-[var(--color-base)] px-4 py-3 text-[var(--color-ink)] outline-none transition-colors focus:border-[var(--color-accent)]"
              ></textarea>
            </div>
            <button
              type="submit"
              class="w-full rounded-lg bg-[var(--color-accent)] px-6 py-3.5 font-semibold text-[#04221d] transition-transform hover:-translate-y-0.5"
            >
              Enviar mensaje
            </button>
            {!formspreeId && (
              <p class="text-center text-xs text-[var(--color-muted)]">
                Configura <code>PUBLIC_FORMSPREE_ID</code> para recibir los
                envíos en tu correo automáticamente.
              </p>
            )}
          </div>
        </form>
          <div
            id="form-exito"
            hidden
            class="rounded-2xl border border-[var(--color-accent)]/30 bg-[var(--color-surface)] p-8 text-center"
          >
            <span class="mx-auto grid h-14 w-14 place-items-center rounded-full bg-[var(--color-accent)]/15 text-[var(--color-accent)]" aria-hidden="true">
              <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
            </span>
            <h3 class="mt-4 text-xl font-semibold">¡Gracias por tu mensaje!</h3>
            <p class="mt-2 text-[var(--color-muted)]">
              Lo hemos recibido y te responderemos en menos de 24 horas.
            </p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <script>
    const form = document.getElementById("contacto-form") as HTMLFormElement | null;
    if (form) {
      const exito = document.getElementById("form-exito");
      const boton = form.querySelector('button[type="submit"]') as HTMLButtonElement;
      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const original = boton.textContent;
        boton.disabled = true;
        boton.textContent = "Enviando…";
        try {
          const resp = await fetch(form.action, {
            method: "POST",
            body: new FormData(form),
            headers: { Accept: "application/json" },
          });
          if (!resp.ok) throw new Error("error");
          form.hidden = true;
          if (exito) {
            exito.hidden = false;
            exito.scrollIntoView({ behavior: "smooth", block: "center" });
          }
          form.reset();
        } catch {
          boton.disabled = false;
          boton.textContent = original;
          alert(
            "No se pudo enviar el mensaje. Inténtalo de nuevo o escríbenos a contacto@lucom.es.",
          );
        }
      });
    }
  </script>

  <Footer />
</Layout>
